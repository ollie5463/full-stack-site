import React from 'react';
import { ErrorHeading } from './NotFoundPage.style';

const NotFoundPage: React.FC = () => <ErrorHeading data-testid="cannot-be-found">This page cannot be found!</ErrorHeading>;

export default NotFoundPage;
